# Vimコマンド チートシート（この教材で使うものだけ）

## 移動
- `h j k l` : 左・下・上・右
- `w` / `b` / `e` : 次の単語の先頭へ / 前の単語の先頭へ / 単語の末尾へ
- `0` / `^` / `$` : 行頭 / 行頭の非空白文字 / 行末
- `gg` / `G` : ファイル先頭 / 末尾
- `f{char}` / `t{char}` : 次の{char}へジャンプ / その手前までジャンプ
- `;` / `,` : 直前の `f`/`t` を同方向 / 逆方向に繰り返す
- `%` : 対応する括弧 `()` `[]` `{}` へジャンプ

## 削除・変更
- `x` / `X` : カーソル位置の1文字削除 / 直前の1文字削除
- `dw` / `de` / `db` : 単語削除（先頭から次の単語頭まで / 末尾まで / 前の単語）
- `dd` : 行削除
- `ciw` / `caw` : 単語を変更（単語のみ / 前後の空白込み）
- `ci'` `ci"` `ci(` `ci{` `ci[` : 引用符・括弧の中身を変更
- `di(` `di{` `di[` : 括弧の中身を削除
- `ct{char}` / `cf{char}` : 指定文字の手前まで変更 / 指定文字まで変更（含む）

## コピー・貼り付け・やり直し
- `yy` / `p` / `P` : 行をヤンク / 直後に貼り付け / 直前に貼り付け
- `u` / `Ctrl-r` : 元に戻す / やり直す
- `.` : 直前の変更を繰り返す

## ビジュアルモード
- `v` / `V` / `Ctrl-v` : 文字単位選択 / 行単位選択 / 矩形（ブロック）選択
- ブロック選択中の `I` / `A` : 選択範囲の各行の先頭 / 末尾に同時入力

## マクロ
- `qa ... q` : レジスタ`a`にマクロを記録開始・終了
- `@a` : マクロを1回再生
- `@@` : 直前に再生したマクロをもう一度再生
- `4@a` : マクロを4回再生

## 検索・置換
- `/pattern` + Enter, `?pattern` + Enter : 前方 / 後方検索
- `n` / `N` : 検索を同方向 / 逆方向に繰り返す
- `:%s/old/new/g` : ファイル全体で置換
